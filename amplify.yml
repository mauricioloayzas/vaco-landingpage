version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run generate
        - aws s3 sync .amplify-hosting/static/ s3://profiles-hayayaku/ --delete --cache-control "max-age=0,no-cache"
  artifacts:
    baseDirectory: .amplify-hosting/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
